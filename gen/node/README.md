# Generated Node (CommonJS / JSPB) types — canonical record

Auto-generated by `buf generate` (protoc-gen-js) from `proto/`. **Do not edit by hand.** This
directory is the canonical generated record.

**To produce/consume Kafka messages, use the SDK — not this package.** `sdk/node` — the
`@truther/contracts` package — bundles the generated (ts-proto) types **plus** the Confluent Schema
Registry serde (`bind` / `produce` / `consume`). See [sdk/node/README.md](../../sdk/node/README.md).

## Adding a contract

Edit `proto/`, then `buf generate` regenerates this directory (CI: `.github/workflows/generate.yml`).
