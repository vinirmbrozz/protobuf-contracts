# Generated TypeScript types — canonical record

Auto-generated by `buf generate` (ts-proto) from `proto/`. **Do not edit by hand.** This directory is
the canonical generated record.

**To produce/consume Kafka messages, use the SDK — not this package.** `sdk/node` — the
`@truther/contracts` package — bundles these ts-proto types **plus** the Confluent Schema Registry
serde (`bind` / `produce` / `consume`). See [sdk/node/README.md](../../sdk/node/README.md).

## Adding a contract

Edit `proto/`, then `buf generate` regenerates this directory (CI: `.github/workflows/generate.yml`).
